#include <iostream>

using std::cin;
using std::cout;
using std::endl;
using std::size_t;

const int MAX_CAPACITY = 100;

/*struct Point
{
    double x, y;
};*/


int main()
{
    unsigned int n;
    double arr[MAX_CAPACITY];
    for(size_t i = 0; i < n * 2; i++)
    {
        cin >> arr[i];
    }
    return 0;
}